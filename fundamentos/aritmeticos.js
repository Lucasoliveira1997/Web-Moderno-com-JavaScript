const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d // in fix
const subtracao = d - b
const mult = a * b
const div = d / a
const modulo = a % 2

console.log(soma, -subtracao, mult, !!div, modulo)

// post fix (a++)
// pre fix (++a)

// b + a (operação binária)
// -a (operação unária)


